extends Node2D
var center_pos = Vector2(360, 1120)
var left_pos = Vector2(120, 1120)
var right_pos = Vector2(580, 1120)
var victory_pos = Vector2(360, -10)
var stage = 1
var playerHP = 3
var stageNumber = 0

# Declare member variables here. Examples:
# var a = 2
# var b = "text"


# Called when the node enters the scene tree for the first time.
func _ready():
	pass

# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
